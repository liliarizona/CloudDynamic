function [dense]=checkDense(point,map,i)
[row col]=size(map);

dense=0;
wPoint=[max(1,point(1)-1) point(2)];
ePoint=[min(row,point(1)+1) point(2)];
sPoint=[point(1) max(1,point(2)-1)];
nPoint=[point(1) min(col,point(2)+1)];

for i=
